import 'package:flutter/material.dart';

class CityToCityRequest extends StatelessWidget {
  const CityToCityRequest({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
